"""Multi-agent orchestration (Phase 12C): intent → decompose → select → execute
(parallel/sequential) → merge → resolve conflicts → validate → unified answer.
Reuses the 12A/12B framework end-to-end; agents stay simple, the planner is smart."""
